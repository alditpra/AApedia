---
// Cloud Partners Section using uploaded official logos
const partners = [
  {
    name: "AWS",
    logo: "/amazon-web-services.svg",
  },
  {
    name: "Google Cloud",
    logo: "/Google_Cloud_logo.svg",
  },
  {
    name: "DigitalOcean",
    logo: "/Digital_Ocean_logo.svg",
  },
  {
    name: "Vultr",
    logo: "/vultr_logo-white.svg",
  },
];
---

<section
  class="py-16 bg-gradient-to-b from-white to-slate-50 border-y border-slate-100"
>
  <div class="max-w-7xl mx-auto px-6">
    <!-- Section Header -->
    <div class="text-center mb-10">
      <p
        class="text-sm font-semibold text-primary-600 uppercase tracking-wider mb-2"
      >
        Infrastruktur Terbaik Dunia
      </p>
      <h2 class="text-2xl md:text-3xl font-bold text-slate-800">
        Powered by Cloud Leaders
      </h2>
      <p class="mt-3 text-slate-500 max-w-2xl mx-auto">
        Kami memilih provider cloud terbaik sesuai kebutuhan dan budget Anda. <br
        />
        Bukan server kaleng-kaleng.
      </p>
    </div>

    <!-- Logo Grid -->
    <div class="grid grid-cols-2 md:grid-cols-4 gap-6">
      {
        partners.map((partner) => (
          <div class="flex items-center justify-center px-6 py-6 rounded-2xl bg-white border border-slate-100 shadow-sm hover:shadow-lg hover:scale-105 transition-all cursor-default">
            <img
              src={partner.logo}
              alt={partner.name}
              class="h-8 w-auto object-contain"
            />
          </div>
        ))
      }
    </div>

    <!-- Cloud Agnostic Highlight -->
    <div class="mt-12 text-center">
      <div
        class="inline-flex items-center gap-3 px-6 py-3 rounded-full bg-gradient-to-r from-primary-50 to-accent-50 border border-primary-100"
      >
        <svg
          class="w-5 h-5 text-primary-600"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            d="M7.5 21L3 16.5m0 0L7.5 12M3 16.5h13.5m0-13.5L21 7.5m0 0L16.5 12M21 7.5H7.5"
          ></path>
        </svg>
        <span class="text-sm font-medium text-slate-700">
          <strong class="text-primary-700">Cloud Agnostic</strong> — Solusi fleksibel,
          tanpa vendor lock-in
        </span>
      </div>
    </div>
  </div>
</section>
